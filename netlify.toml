[build]
  publish = "dist/hangman-angular"
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-angular"
  # Chặn tự động runtime plugin bằng cách disable auto detection
  [plugins.inputs]
    skipRuntimeInjection = true